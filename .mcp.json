{
  "mcpServers": {
    "gopilot-tools": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/servers/gopilot-tools/dist/bundle.js"],
      "env": {
        "GOPILOT_FRONTEND": "${GOPILOT_FRONTEND:-svelte}"
      }
    }
  }
}
