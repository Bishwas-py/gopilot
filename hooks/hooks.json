{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -q '_handlers\\.go$' && echo '⚡ Go handler changed — run /gopilot:sdk to regenerate frontend SDK' || true"
          }
        ]
      }
    ]
  }
}
